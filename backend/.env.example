# Runtime mode: development, test, or production.
NODE_ENV=development

# Local Express port. The frontend example expects port 4000.
PORT=4000

# Replace every placeholder with values from a developer-owned MongoDB Atlas database.
# Never commit the resulting .env file.
MONGODB_URI=mongodb+srv://shahzaibalijamro_db_user:O2MPSMAcH6eLCCBF@cluster0.ayasoa9.mongodb.net/?appName=Cluster0